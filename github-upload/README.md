# Sanqiancai Order

三千彩环保色浆订购系统。

## Render environment variables

Set these passwords in Render before public use:

- `SALES_PASSWORD`
- `ASSISTANT_PASSWORD`
- `OWNER_PASSWORD`
- `STOCK_PASSWORD`
- `ADMIN_PASSWORD`

Local development uses `123456` when `NODE_ENV` is not `production`.
